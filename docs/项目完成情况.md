# 项目完成情况审查报告

> 审查日期：2026-06-29  
> 审查范围：后端 `server/`、前端 `frontend/`、数据库脚本 `sql/`、项目文档 `README.md`、`TODO.md`、`docs/`、本地启动脚本。  
> 审查方式：静态代码核对、文档核对、后端 Maven 测试命令、前端生产构建命令。

## 1. 总体结论

宿舍报修管理系统已经具备课程验收所需的核心业务闭环：学生提交报修、管理员审核派单、维修人员处理、学生确认评价、管理员统计和用户管理均已有前后端实现，数据库脚本和演示账号也已准备。

按课程验收口径估算：

| 模块 | 完成度 | 结论 |
| --- | --- | --- |
| 后端接口 | 95% | 主流程、权限、统计、上传接口已实现；公开注册权限、角色限制、状态流转和手机号校验已补单元测试。 |
| 前端页面 | 92% | 多角色页面、列表筛选、详情操作、统计和上传控件已实现；注册模式已明确只面向学生，仍缺完整验收截图。 |
| 数据库脚本 | 90% | 建表、演示账号和多状态演示数据已具备；`repair_orders.status` 已补数据库默认值。 |
| 文档与材料 | 88% | README、API、部署、需求、流程、接口测试材料和课程报告已更新；最终截图和真实联调材料仍需补齐。 |
| 整体可演示度 | 90%-92% | 主流程可以演示，但提交前仍需最终联调复测、图片上传实测和验收截图。 |

## 2. 本次验证结果

| 命令 | 目录 | 结果 | 说明 |
| --- | --- | --- | --- |
| `mvn test` | `server/` | 通过 | 已执行 11 个 JUnit/Mockito 单元测试，0 失败、0 错误。 |
| `npm run build` | `frontend/` | 通过 | TypeScript 检查和 Vite 生产构建均成功。 |

本次未重新启动 MySQL 执行端到端接口回归，也未重新上传图片验证 `/uploads/...` 静态访问。验收前需要补做一次完整联调。

## 3. 已完成能力

### 3.1 后端

- Spring Boot 3 + Java 17 项目结构完整。
- MyBatis XML Mapper、实体、DTO、VO、Service、Controller 分层清晰。
- JWT 登录鉴权、`@RequireRole` 角色权限校验和统一响应 `ApiResponse<T>` 已实现。
- 学生报修：新增、我的报修、详情、修改待受理工单、撤销、确认、评价。
- 管理员：全部工单、审核、驳回、派单、用户管理、软删除、统计。
- 维修人员：我的任务、开始处理、提交完成、申请重派。
- 工单详情包含处理时间线和评价信息。
- 图片上传接口 `/api/uploads/images` 和 `/uploads/**` 静态资源映射已实现。

### 3.2 前端

- React 19 + TypeScript + Vite 项目可构建。
- Axios API 封装已覆盖认证、报修、用户、统计和上传接口。
- 登录/注册、学生首页、提交报修、我的报修、进度跟踪、工单详情、管理员首页、派单、用户管理、个人中心、公告页已拆分到 `pages/`。
- 通用布局、弹窗、状态徽章、分页、筛选、图表、时间线等组件已拆分到 `components/`。
- JWT token 本地保存、请求拦截、角色首页跳转和错误提示已实现。
- 图片上传控件已接入上传接口。
- 注册模式已隐藏演示账号切换，并明确公开注册仅面向学生账号。

### 3.3 数据库与演示数据

- `sql/init.sql` 可创建 `dorm_repair` 数据库及四张核心表；`sql/demo-data.sql` 载入完整验收演示数据。
- 初始化脚本内置学生、管理员、维修人员三个演示账号，密码均为 `123456`。
- `sql/demo-data.sql` 覆盖全部 9 种状态（PENDING / APPROVED / ASSIGNED / PROCESSING / WAIT_CONFIRM / COMPLETED / REJECTED / CANCELLED / NEED_REASSIGN）的演示工单。
- `repair_orders.status` 已在初始化脚本中设置 `DEFAULT 'PENDING'`。
- 软删除字段和索引已覆盖用户、工单等核心查询场景。

### 3.4 文档与测试材料

- `README.md` 已补充当前状态、前端启动、环境变量、一键启动和文档索引。
- `docs/API.md` 已说明统一响应和业务 `code` 口径，并更新演示数据脚本命令。
- `docs/deploy.md` 已补充前端、配置项、一键启动、常见问题和验收前验证命令。
- `docs/api-tests/INDEX.md` 已说明既有测试结果的状态码口径。
- `TODO.md` 已按本次审查更新验收前剩余事项。
- `server/src/test/java/` 已新增 11 个单元测试。

## 4. 主要未完成项与风险

| 优先级 | 问题 | 影响 | 建议 |
| --- | --- | --- | --- |
| P0 | 最终端到端复测未完成 | 无法证明当前机器数据库、后端、前端和上传功能全部可运行 | 重新执行初始化脚本，启动前后端，按三类账号跑完整流程并保存结果。 |
| P0 | 验收截图和个人信息未补齐 | 影响课程提交材料完整性 | 在 `docs/课程报告.md` 中补姓名、学号、班级、指导教师，并补登录、报修、详情、派单、维修、确认评价、统计、数据库和接口测试截图。 |
| P1 | 图片上传缺最终联调证据 | 接口和前端已接入，但缺验收截图 | 实测上传、保存 `imageUrls`、访问 `/uploads/...`。 |

## 5. 验收前建议顺序

1. 执行 `sql/init.sql` 和 `sql/demo-data.sql` 初始化数据库。
2. 启动后端和前端，使用 `student001`、`admin001`、`repair001` 跑完整主流程。
3. 实测图片上传、静态访问 `/uploads/...` 和报修单 `imageUrls` 保存。
4. 保存前端页面、数据库表、接口返回和构建命令截图。
5. 填写课程报告封面个人信息，并替换或补齐最终截图。

## 6. 当前演示主线

```text
学生登录 -> 提交报修 -> 管理员审核 -> 管理员派单 -> 维修人员开始处理 -> 维修人员提交完成 -> 学生确认完成 -> 学生评价 -> 管理员查看统计
```

演示账号：

| 角色 | 账号 | 密码 |
| --- | --- | --- |
| 学生 | `student001` | `123456` |
| 管理员 | `admin001` | `123456` |
| 维修人员 | `repair001` | `123456` |
