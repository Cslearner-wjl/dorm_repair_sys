# 宿舍报修管理系统 TODO

> 更新时间：2026-06-29
> 更新依据：本次静态审查 + `mvn test` + `npm run build` + 既有 `docs/api-tests/` 接口测试材料。
> 勾选口径：`[x]` 表示已实现并能从代码、构建结果或测试材料中确认；`[ ]` 表示未完成、未复测或缺少验收材料。

## 0. 当前验收结论

系统已进入 **验收前收尾阶段**。后端主流程、数据库脚本、前端页面和接口调用都已经具备，前端不再是“待开发”状态。

按课程完整验收口径估算：

- **核心系统可演示度：约 85% - 90%**。登录、报修、审核、派单、维修、确认、评价、统计、用户管理、图片上传接口均已实现或接入。
- **完整提交包就绪度：约 75% - 80%**。主要差距在验收截图、课程报告、最终联调复测、配置安全和少量高优先级缺陷。
- 修完 P0 项并补齐截图/报告后，整体可提升到 **95% 左右**，剩余 P1/P2 可作为答辩时的优化项说明。

当前最重要的结论：

- [x] 后端 Spring Boot + MyBatis + MySQL + JWT 主流程接口已实现。
- [x] 前端 React + Vite 已实现，且通过 Axios 连接真实后端 API。
- [x] `frontend` 执行 `npm run build` 成功。
- [x] `server` 执行 `mvn test` 成功。
- [ ] 后端当前没有 `server/src/test` 测试源码，`mvn test` 只是编译通过，不能算自动化业务回归。
- [x] 既有 curl/API 测试结果已保存到 `docs/api-tests/`。
- [ ] 本次未重新跑本机 MySQL 端到端接口联调，需要验收前复测一次。
- [ ] 课程报告、最终截图、数据库截图和演示材料仍需补齐。

## 1. 本次审查发现的 P0 问题

以下项会直接影响“完整验收”或代码提交质量，建议先修。

- [ ] **公开注册权限漏洞**：`RegisterDTO` 暴露 `role` 字段，`AuthServiceImpl.register` 只禁止 `ADMIN`，请求体传 `role=REPAIR` 时可能公开注册维修人员账号。需求文档要求公开注册只能创建学生账号。
  修复建议：公开注册直接强制 `STUDENT`，或拒绝任何非空/非 `STUDENT` 的 `role`；补一条接口测试。

- [x] **默认数据库密码疑似真实本机密码**：`server/src/main/resources/application.yml` 曾出现非示例默认 `DB_PASSWORD`，不符合“不提交真实凭据”的要求，且 README/deploy 文档写的是 `root`。
  已处理：默认值已恢复为 `${DB_PASSWORD:root}`，真实本机密码未纳入提交。

- [ ] **最终验收截图缺失**：目前只看到 `frontend/screenshots/student-home-desktop.png`，还缺登录、注册、提交报修、我的报修、详情、管理员派单、维修人员处理、学生确认评价、统计页、权限拦截、数据库表结构和示例数据截图。

- [ ] **课程报告未完成**：需按任务书补齐 3000 字以上正文、系统截图、数据库设计、接口说明、测试结果、团队分工、总结、参考文献和附录。

- [ ] **最终端到端复测未完成**：验收前需要重新执行 `sql/init.sql`、`sql/demo-data.sql`、启动后端和前端，用三个演示账号完整跑一遍主流程，并保存复测记录。

## 2. P1 验收前建议处理

- [ ] `RepairCreateDTO.contactPhone` 只做了 `@NotBlank`，建议补手机号格式 `@Pattern`；`RepairUpdateDTO` 继承后可同步生效。
- [ ] `repair_orders.status` 在 SQL 层补默认值 `DEFAULT 'PENDING'`，当前只由 Service 设置。
- [ ] 增加至少 4-8 个后端自动化测试：公开注册只能学生、学生不可访问管理员接口、完整状态流转、已完成工单不可再次派单、非创建学生不可确认/评价、非分配维修人员不可处理。
- [ ] 统一“HTTP 状态码 vs JSON code”说明：当前异常统一 HTTP 200，业务码放在 `ApiResponse.code`，前端能处理，但报告/API 文档需要明确。
- [ ] 前端注册/登录页的角色切换更像演示账号入口，注册模式下容易被误解为可选择角色；建议注册模式只展示学生注册或加清晰文案。
- [ ] 图片上传已接入前端和后端，但需要验收前实测上传、静态访问 `/uploads/...`、报修单保存 `imageUrls`。
- [ ] `docs/API.md` 中 demo-data 执行命令仍有旧绝对路径示例，建议改成当前仓库相对命令。
- [ ] TODO 旧版本写“设计、需求、流程、课程任务书等文档归档到 docs/”，但当前 `design.md`、`design-qa.md`、`课程任务书.docx` 仍在仓库根目录；要么移动到 `docs/`，要么更新文档清单。
- [ ] README 只写了后端启动，需补前端启动/构建命令、`VITE_API_BASE_URL`、CORS 配置说明。

## 3. 已完成模块清单

### 3.1 后端

- [x] Spring Boot 3 + Java 17 配置。
- [x] MyBatis + MySQL 数据访问。
- [x] JWT 登录鉴权。
- [x] `@RequireRole` 角色权限校验。
- [x] 统一响应 `ApiResponse<T>`。
- [x] 统一异常处理。
- [x] 用户注册、登录、当前用户。
- [x] 管理员用户管理：创建、列表、维修人员列表、修改信息、修改角色、启用/禁用、软删除。
- [x] 学生报修：创建、我的报修、详情、修改待受理工单、撤销、删除允许范围内的工单。
- [x] 管理员工单：全部列表、审核、驳回、派单、删除。
- [x] 维修人员工单：我的任务、开始处理、提交完成、申请重派。
- [x] 学生确认完成和评价。
- [x] 处理记录时间线。
- [x] 管理员统计：概览、状态、类型、楼栋、最近 N 天、维修人员统计。
- [x] 图片上传接口 `/api/uploads/images`。
- [x] CORS 配置和上传静态资源映射。

### 3.2 前端

- [x] Vite + React + TypeScript 项目已存在于 `frontend/`。
- [x] Axios API 封装已接入后端接口。
- [x] JWT token 本地保存和请求拦截已实现。
- [x] 登录/注册界面。
- [x] 学生首页、提交报修、我的报修、详情、确认评价。
- [x] 管理员首页、工单管理、派单、统计、用户管理。
- [x] 维修人员任务列表、详情处理、完成、申请转派。
- [x] 图片上传控件已接入上传接口。
- [x] `npm run build` 已通过。
- [ ] 缺少完整页面截图和真实联调录屏/截图材料。

### 3.3 数据库与脚本

- [x] `sql/init.sql` 建库建表。
- [x] `users`、`repair_orders`、`repair_records`、`repair_feedbacks` 表。
- [x] 外键、唯一索引、普通索引。
- [x] 三类演示账号。
- [x] `sql/demo-data.sql` 可恢复多状态演示数据。
- [ ] `repair_orders.status` 默认值待补。
- [ ] 验收前需要在本机或演示机重新执行并截图。

### 3.4 文档与测试材料

- [x] `README.md`。
- [x] `docs/API.md`。
- [x] `docs/deploy.md`。
- [x] `docs/需求文档.md`。
- [x] `docs/开发流程.md`。
- [x] `docs/api-tests/` 接口测试 JSON 和索引。
- [x] `design-qa.md` 已记录一次视觉 QA 结论。
- [ ] 课程报告。
- [ ] 最终部署验证截图。
- [ ] 最终数据库截图。
- [ ] 最终前端全流程截图。

## 4. 验收材料清单

### 4.1 页面截图

- [ ] 登录页。
- [ ] 注册页。
- [x] 学生首页桌面截图：`frontend/screenshots/student-home-desktop.png`。
- [ ] 学生提交报修页。
- [ ] 我的报修列表页。
- [ ] 报修详情页，含处理记录时间线。
- [ ] 管理员首页/统计页。
- [ ] 管理员工单审核页。
- [ ] 管理员派单页。
- [ ] 维修人员任务页。
- [ ] 维修人员提交完成页。
- [ ] 学生确认完成和评价页。
- [ ] 用户管理页。
- [ ] 权限拦截或无权限提示页。
- [ ] 图片上传成功页。
- [ ] 移动端关键页面截图，至少登录页、学生首页、提交报修。

### 4.2 数据库截图

- [ ] `users` 表结构和演示账号数据。
- [ ] `repair_orders` 表结构和多状态工单数据。
- [ ] `repair_records` 时间线数据。
- [ ] `repair_feedbacks` 评价数据。
- [ ] 软删除字段 `deleted_at` 示例。

### 4.3 接口与测试材料

- [x] 登录、注册、权限、异常、状态流转、统计等接口 JSON 结果已保存。
- [ ] 补充公开注册不能注册维修人员的测试结果。
- [ ] 补充图片上传接口测试结果。
- [ ] 补充验收前最终回归记录：命令、时间、环境、结果。
- [ ] 如不写自动化测试，报告中说明目前以 curl/API 回归为主；更推荐补少量 JUnit 测试。

## 5. 当前最高优先级

1. 修复公开注册角色漏洞，并补测试记录。
2. 重新初始化数据库，启动后端和前端，跑完整主流程。
3. 截取完整验收截图，整理到 `docs/screenshots/` 或 `frontend/screenshots/`。
4. 完成课程报告和附录材料。
5. 补少量自动化测试或至少补最终回归测试记录。

## 6. Bug 与风险记录

| 日期 | 问题 | 严重程度 | 处理状态 |
| --- | --- | --- | --- |
| 2026-06-29 | 公开注册可通过请求体传 `role=REPAIR` 创建维修人员账号 | P0 | 待修复 |
| 2026-06-29 | `application.yml` 默认数据库密码疑似真实本机密码，且与 README 不一致 | P0 | 已修复 |
| 2026-06-29 | 后端没有测试源码，`mvn test` 结果为 `No tests to run` | P1 | 待补测试 |
| 2026-06-29 | 报修联系电话缺少手机号格式校验 | P1 | 待修复 |
| 2026-06-29 | TODO/文档旧状态仍写前端待开发、部分文档归档路径不准确 | P1 | TODO 已更新，其他文档待同步 |
| 2026-06-28 | `RegisterDTO` 手机号缺少格式校验 | P1 | 已修复 |
| 2026-06-28 | `UserCreateDTO` 手机号缺少格式校验 | P1 | 已修复 |
| 2026-06-28 | `UserUpdateDTO` 手机号缺少格式校验 | P1 | 已修复 |

## 7. P2 扩展项

P0/P1 完成前不要优先扩展。

- [ ] 消息通知/公告后端接口。
- [ ] Excel 导出。
- [ ] 报修超时提醒。
- [ ] 操作日志管理。
- [ ] 评价修改或隐藏违规评价。
- [ ] 更复杂的统计图表。
- [ ] 更完整的移动端视觉 QA。

## 8. 最终演示主线

```text
学生登录 -> 提交报修 -> 管理员审核 -> 管理员派单 -> 维修人员处理 -> 维修人员提交完成 -> 学生确认 -> 学生评价 -> 管理员查看统计
```

演示账号：

```text
学生：student001 / 123456
管理员：admin001 / 123456
维修人员：repair001 / 123456
```
